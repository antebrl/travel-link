import 'package:flutter/material.dart';

class ParticipantsPreview extends StatelessWidget {
  const ParticipantsPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
